<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Background and surface brushes -->
    <SolidColorBrush x:Key="AppBackground"    Color="#1E1E2E"/>
    <SolidColorBrush x:Key="AppSurface"       Color="#2A2A3E"/>
    <SolidColorBrush x:Key="AppSurfaceDark"   Color="#12121E"/>

    <!-- Text brushes -->
    <SolidColorBrush x:Key="AppText"          Color="White"/>
    <SolidColorBrush x:Key="AppTextMuted"     Color="#AAAACC"/>
    <SolidColorBrush x:Key="AppTextSubtle"    Color="#CCCCEE"/>
    <SolidColorBrush x:Key="AppTextDim"       Color="#888899"/>

    <!-- Accent brushes -->
    <SolidColorBrush x:Key="AppAccent"        Color="#6C63FF"/>
    <SolidColorBrush x:Key="AppAccentHover"   Color="#8075FF"/>
    <SolidColorBrush x:Key="AppAccentPressed" Color="#5248CC"/>

    <!-- Border brushes -->
    <SolidColorBrush x:Key="AppBorder"        Color="#555577"/>
    <SolidColorBrush x:Key="AppBorderDark"    Color="#333355"/>

    <!-- Status brushes -->
    <SolidColorBrush x:Key="AppError"         Color="#FF6B6B"/>

    <!-- Semi-transparent loading overlay -->
    <SolidColorBrush x:Key="AppOverlay"       Color="#CC1E1E2E"/>

    <!-- Primary action button (accent-filled) -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background"       Value="{DynamicResource AppAccent}"/>
        <Setter Property="Foreground"       Value="{DynamicResource AppText}"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Padding"          Value="12,6"/>
        <Setter Property="FontSize"         Value="13"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppAccentHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppAccentPressed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary / ghost button (outlined) -->
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="Foreground"       Value="{DynamicResource AppTextMuted}"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource AppBorder}"/>
        <Setter Property="BorderThickness"  Value="1"/>
        <Setter Property="Padding"          Value="12,6"/>
        <Setter Property="FontSize"         Value="13"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppSurface}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AppText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
